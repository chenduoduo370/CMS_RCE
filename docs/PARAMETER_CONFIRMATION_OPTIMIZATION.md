# 参数确认流程优化

## 问题描述
用户反馈："在参数确认部分，需要让我确认一下"

**观察**：AI 在"请确认参数"时就已输出了 ##AUTOTEST## 标记，没有等用户真正确认。

## 根本原因
SYSTEM_PROMPT 中的规则说"只在用户最终确认后输出"，但措辞不够强硬，AI 仍然在参数确认阶段就输出了标记。

## 解决方案

强化 SYSTEM_PROMPT 的规则，使用强制性措辞和明确的时间点控制。

### 优化前
```
步骤3：收集完成后确认参数
  - 向用户展示即将执行的测试参数汇总（包括端口信息）
  - 询问用户是否确认执行

当用户确认执行后，必须在回复的最后输出...
##AUTOTEST##...##END##
```

**问题**：
- "当用户确认执行后"理解模糊
- AI 将参数确认视为"已确认"

### 优化后
```
步骤3：收集完成后确认参数
  - 向用户展示即将执行的测试参数汇总（包括端口信息）
  - 询问用户是否确认执行
  - 【重要】此时仅展示参数确认，不要输出任何标记或JSON

当用户明确回复"是"、"确认"、"开始"、"执行"等确认词后，
必须在回复的最后输出以下精确格式的标记...

重要提醒：
  - 禁止在参数确认阶段输出 ##AUTOTEST## 标记
  - 仅在用户明确确认（输入确认词）后才输出标记
  - 标记必须是回复的最后一行，标记后面不能有任何文字

【严格规则】参数确认阶段绝对禁止输出任何 ##AUTOTEST## 标记
```

**改进**：
- "绝对禁止" → 强制性
- "明确回复确认词" → 清晰的触发条件
- "仅在用户明确确认后" → 明确的时间点
- 【严格规则】标签 → 强调硬性要求

## 正确的交互流程

### 第1步：参数确认（无标记）
```
AI: 请确认以下参数：
    - 目标：192.168.10.111
    - 端口：[82, 83]
    - 扫描：否
    确认执行吗？
```

**注意**：此时 AI **不输出** ##AUTOTEST## 标记

### 第2步：用户确认
```
用户: 确认执行
或
用户: 是
或
用户: 开始
```

### 第3步：AI 输出标记
```
AI: [其他回复内容...]

##AUTOTEST##{"host":"192.168.10.111",...}##END##
```

**注意**：标记在最后一行，后面没有任何文字

## Git 提交

```
a84b94c 优化：SYSTEM_PROMPT 参数确认阶段禁止输出标记
```

## 技术细节

关键改动：
1. 添加明确禁止：在参数确认时禁止输出标记
2. 添加触发条件：用户明确回复确认词后才输出
3. 添加【严格规则】标签：确保 AI 理解这是硬性要求
4. 明确列举确认词：是、确认、开始、执行等

## 测试方法

用户可以重新进行测试：

```
1. 输入: 我要渗透 192.168.10.111 端口是 82 83
2. AI: [参数确认，无标记]
3. 输入: 确认执行
4. AI: [其他内容...] 然后输出标记
```

---

**修改日期**：2026-02-28
**影响范围**：SYSTEM_PROMPT 仅
**效果**：参数确认流程现已正确分离